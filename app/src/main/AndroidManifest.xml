<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.dbcorp.textorapido">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="br.com.dbcorp.textorapido.atividades.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.com.dbcorp.textorapido.atividades.TextoActivity"
            android:label="@string/title_activity_insert" >
        </activity>
        <activity
            android:name="br.com.dbcorp.textorapido.FileActivity"
            android:label="@string/title_activity_file" >
        </activity>
    </application>

</manifest>
